# To-do

- **Copy schedule as Markdown (Paper.nu).** A button that copies the current schedule as Markdown (courses, sections, days/times, instructors, CTEC ratings where cached) so it can be pasted into Claude or other tools.
