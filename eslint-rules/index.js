// Local ESLint plugin: better-caesar (bc-rules) custom rules.
import noRawActionButton from "./no-raw-action-button.js";

export default {
  rules: {
    "no-raw-action-button": noRawActionButton
  }
};
